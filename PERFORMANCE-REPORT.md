# 逐亚 Hop Hop 2.0：手机性能与音效验证报告

验证日期：2026-09-01

## 本次处理范围

- 优化手机端 Canvas 分辨率、渲染路径、屏外裁剪和每帧内存分配。
- 专项检查猫/狗螺旋桨飞行与御剑飞行。
- 移除螺旋桨与御剑飞行的持续循环音效，避免移动端循环播放不流畅；其余短音效保持不变。
- 不调整跳跃高度、重力、移动速度、道具持续时间、概率和碰撞规则。
- 不修改原始 PNG 素材文件，不覆盖原版项目。

## 浏览器性能采样

测试环境为 Codex 内置 Chromium 的手机尺寸模拟：CSS 约 `390 × 844`、设备像素比
`1.5`、Canvas 内部分辨率 `585 × 1266`。每个状态先预热 350 ms，再采样 1.5 秒。

| 状态 | 平均 FPS | 平均帧时 | P95 帧时 | P99 帧时 | 超过 33.3 ms |
| --- | ---: | ---: | ---: | ---: | ---: |
| 普通跳跃 | 42.43 | 23.57 ms | 50.1 ms | 83.4 ms | 11 |
| 猫/狗螺旋桨飞行 | 60.05 | 16.65 ms | 16.8 ms | 17.5 ms | 0 |
| 御剑飞行 | 60.04 | 16.66 ms | 16.9 ms | 17.5 ms | 0 |

本次专项采样中，两种飞行状态都稳定在约 60 FPS，且没有超过 33.3 ms 的长帧。
普通状态的短采样会受浏览器调度和世界生成影响，因此这些数据用于回归判断，不等同于
真实手机的硬件测试结果；上线后仍建议在一台中低端安卓机和一台 iPhone 上各试玩一次。

## 自动回归结果

36 项浏览器测试全部通过，覆盖：

- 375 × 667、390 × 844、430 × 932 三种手机尺寸的 Canvas 分辨率与 DPR 上限。
- 资源预缩放缓存、镜头插值、世界坐标渲染和数组原地清理。
- 螺旋桨平滑进入/结束、连续位置、御剑/护盾相关状态及高速落台碰撞。
- 30 次连续落台、20 次手机端重开和可见平台密度。
- 两种飞行循环音效均未注册、未请求，剩余 12 个短音效文件全部可用。

## 素材与音效审计

原始角色、道具、护盾和怪物 PNG 明显大于实际显示尺寸。2.0 在内存中建立预缩放
Canvas 缓存，避免飞行期间反复缩放大图；源 PNG 文件未改动。

`propeller-loop.wav` 与 `sword-loop.wav` 已删除，运行时也不会生成或恢复这两种循环声音。
跳跃、弹簧、拾取、奖章、护盾、射击、命中、破裂、死亡、开始和御剑启动提示音保持原样。

## 主要实现点

- Canvas DPR 上限 1.5，按实际 CSS 宽度而不是固定逻辑宽度建立 backing store。
- 58 个左右的角色/道具/敌人绘制资源按当前角色组合预缩放缓存。
- 平台、敌人、奖章、子弹与特效屏外剔除。
- 高频筛选、排序和数组重建改为单次循环与原地压缩。
- 摄像机位置使用上一物理帧与当前物理帧做渲染插值。
- Web Audio 优先解码剩余 WAV；HTML Audio 继续作为移动端兼容路径。

